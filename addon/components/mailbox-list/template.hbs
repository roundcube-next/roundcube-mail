<ul class="list-group">
  {{#each mailboxes as |mailbox|}}
    {{#link-to 'shell.mail.mailbox' mailbox.name class='list-group-item' tagName='li'}}
    {{prettify mailbox.name}}
      {{#if mailbox.unreadThreads}}
        <span class="badge">{{mailbox.unreadThreads}}</span>
      {{/if}}
    {{/link-to}}
  {{/each}}
</ul>
